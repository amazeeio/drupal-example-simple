docker-compose-yaml: docker-compose.yml

project: drupal9-example-simple

tasks:
#   pre-rollout:
#     - run:
#         name: drush sql-dump
#         command: mkdir -p /app/web/sites/default/files/private/ && drush sql-dump --ordered-dump --gzip --result-file=/app/web/sites/default/files/private/pre-deploy-dump.sql.gz || true
#         service: cli

  post-rollout:
    - run:
        name: install demo_umami profile
        command: drush -y si demo_umami
        service: cli



environments:
  9.x:
    routes:
      - nginx:
        - vu.au.amazee.io
    cronjobs:
      - name: drush cron
        schedule: "*/15 * * * *"
        command: drush cron
        service: cli
